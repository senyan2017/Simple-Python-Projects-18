#!/usr/bin/env python
# coding: utf-8
"""Reusable core logic for the simple projects.

Each module here holds the pure, importable part of a project (calculation,
password generation, quiz scoring, board checks). Nothing in this package runs
on import, so the logic can be reused or tested without the command-line flow.
The matching scripts in the repository root provide the interactive entry points.
"""
